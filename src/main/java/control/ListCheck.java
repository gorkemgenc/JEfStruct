package control;

public class ListCheck {
    //IsUnique Array - ascii and unicode için farklı yap - type bazlı bak - string içinde bak - object varsa durum farklı
    //String deduplication (each of one is unique)
    //Class contains this class
    //Class types checking is equal all types is equal
    // Ispermutation -> array method, ascii bit manipulation method for lowercase
    // Class is equals or not (inner of inner)
    // Maybe reflection with types checking
}
